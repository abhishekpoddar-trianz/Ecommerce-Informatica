@page
@model EcommerceInformatica.Web.Pages.City.EditModel
@{
    ViewData["Title"] = "Edit City";
}

<div class="container mt-4">
    <div class="row mb-3">
        <div class="col-md-6">
            <h2><i class="bi bi-pencil"></i> Edit City</h2>
        </div>
        <div class="col-md-6 text-end">
            <a asp-page="Index" class="btn btn-secondary">
                <i class="bi bi-arrow-left"></i> Back to List
            </a>
        </div>
    </div>

    <div class="card shadow-sm">
        <div class="card-header bg-warning">
            <h5 class="mb-0">Edit City Information</h5>
        </div>
        <div class="card-body">
            <form method="post">
                <input type="hidden" asp-for="City.Id" />
                <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                <div class="mb-3">
                    <label asp-for="City.Name" class="form-label"></label>
                    <input asp-for="City.Name" class="form-control" />
                    <span asp-validation-for="City.Name" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="City.ProvinceId" class="form-label"></label>
                    <select asp-for="City.ProvinceId" class="form-select" asp-items="Model.Provinces">
                        <option value="">-- Select Province --</option>
                    </select>
                    <span asp-validation-for="City.ProvinceId" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <div class="form-check">
                        <input asp-for="City.IsActive" class="form-check-input" type="checkbox" />
                        <label asp-for="City.IsActive" class="form-check-label"></label>
                    </div>
                </div>

                <hr />

                <div class="row">
                    <div class="col-md-6">
                        <p class="text-muted small">
                            <strong>Created:</strong> @Model.City.CreatedDate.ToString("MMM dd, yyyy HH:mm") by @Model.City.CreatedBy
                        </p>
                    </div>
                    @if (Model.City.ModifiedDate.HasValue)
                    {
                        <div class="col-md-6">
                            <p class="text-muted small">
                                <strong>Last Modified:</strong> @Model.City.ModifiedDate.Value.ToString("MMM dd, yyyy HH:mm") by @Model.City.ModifiedBy
                            </p>
                        </div>
                    }
                </div>

                <div class="mt-4">
                    <button type="submit" class="btn btn-warning">
                        <i class="bi bi-save"></i> Save Changes
                    </button>
                    <a asp-page="Index" class="btn btn-secondary">
                        <i class="bi bi-x-circle"></i> Cancel
                    </a>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
